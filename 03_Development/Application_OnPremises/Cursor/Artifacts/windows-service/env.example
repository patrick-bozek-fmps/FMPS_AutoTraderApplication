# Example environment overrides for the FMPS AutoTrader Windows service
# Lines starting with # are ignored.
# Values are injected as Windows service environment variables via Apache Procrun.

# Application network configuration
FMPS_APP_HOST=0.0.0.0
FMPS_APP_PORT=8080

# API authentication (replace with secure keys)
FMPS_API_KEY=change-me
FMPS_METRICS_API_KEY=change-me

# Optional database override (defaults to SQLite under %FMPS_DATA_DIR%)
# FMPS_DB_URL=jdbc:sqlite=C:/ProgramData/FMPSAutoTrader/data/autotrader.db

